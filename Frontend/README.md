# Frontend — Resume Optimizer

This is a minimal Vite + React frontend that calls the FastAPI backend endpoints.

Run locally:

```bash
cd Frontend
npm install
npm run dev
```

The dev server proxies `/api/*` to `http://127.0.0.1:8000` (see `vite.config.js`).

Open: http://localhost:5173
